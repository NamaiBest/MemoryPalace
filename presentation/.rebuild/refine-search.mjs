import {chromium} from 'playwright';
const b=await chromium.launch({headless:true,executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'});const p=await b.newPage({viewport:{width:1600,height:900}});await p.goto('http://localhost:3000/explore',{waitUntil:'networkidle'});await p.locator('input').first().fill('laptop keyboard');await p.waitForTimeout(2000);try{await p.getByText('SEARCHING COGNITIVE MEMORY...', {exact:false}).waitFor({state:'hidden',timeout:45000});}catch{};await p.evaluate(()=>window.scrollTo(0,330));await p.screenshot({path:'presentation/revised/assets/search-keyboard.png'});console.log((await p.locator('body').innerText()).slice(0,1100));await b.close();
